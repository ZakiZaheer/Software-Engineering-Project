name: task_manager
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  sqflite: ^2.4.0
  supabase_flutter: ^2.6.0
  intl:
  flutter_local_notifications:
  flutter_tts:
  workmanager:
  speech_to_text: ^6.3.0
  regexed_validator: ^2.0.0
  path_provider:
  share_plus:

dev_dependencies:
  flutter_test:
    sdk: flutter
  intl: ^0.18.0
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
